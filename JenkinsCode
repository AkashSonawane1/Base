pipeline{
      agent any
      stages{
        stage('Build'){
          steps{
          sh 'echo "Hello World"'
          sh '''
          "multiline shell steps working as well"
          ls -lah
          '''
          }
         }
       }
      }
